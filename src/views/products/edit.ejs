
<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs') %>
    <body>
        <%- include('../partials/header.ejs') %>
        <main>
            <h3 class="titulo">Modificá un Producto</h3>
            <form method="POST" action="/products/update/<%= product.id %>" class="formRegistro" enctype="multipart/form-data">
                <input type="hidden" name="id" value="<%= product.id %>">
                <div>
                    <label for="name" class="labelForm">Nombre del Producto</label>
                    <!-- product name error -->
                    <% if(locals.errors && errors.name){ %>
                        <div style="color: red;">
                            <%= errors.name.msg %>
                        </div>
                    <% } %>
                    <input type="text" class="inputRegistro" name="name" id="name" value="<%= product.name %>" >
                </div>
                <div>
                    <label for="description" class="labelForm">Descripción</label>
                   
                    <input type="text" class="inputRegistro" name="description" id="description" value="<%= product.description %>" >
                </div>

                <div>
                    <label for="image" class="labelForm">Foto del Producto</label>
                    
                    <input type="file" class="inputRegistro" name="image" id="product-image" >
                </div>
                
                <div>
                    <label for="category" class="labelForm">Categoría</label>
                    
                    <input type="text" class="inputRegistro" name="category" id="category" value="<%= product.category %>" >
                </div>
    
                <div>
                    <label for="price" class="labelForm">Precio</label>
                    <!-- product price error -->
                    
                    <input type="text" class="inputRegistro" name="price" id="price" value="<%= product.price %>" >
                </div>
    
    
                <div class="buttonContainer">
                    <button type="submit">Editar Producto</button>
                </div>
            </form>
        </main>
        <%- include('../partials/footer.ejs') %>
    </body>
</html>
